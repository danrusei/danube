[workspace.lints.rust]
unsafe_code = "forbid"
unused = "allow" # for experimental dev

[workspace]
resolver = "2"
members = [
    # -- Danube stream core services
    "danube-broker",
    "danube-controller",

    # -- Danube lib client
    "danube-client",
]
